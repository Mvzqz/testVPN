name: Generate List Example

on:
  workflow_dispatch:
    inputs:
      start-index:
        description: 'Starting list index'
        required: true
        type: number
      userCount:
        description: 'number of users to create'
        required: true
        type: number

jobs:
  generate_list:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install

      - name: Generate List
        id: generate
        run: |
          n=${{ github.event.inputs.userCount }}
          list=()
          for ((i=1; i<=$n; i++)); do
            list+=("\"$i/$n\",")
          done
          json_list="[${list[@]}]"
          echo json_list
          echo "list::\"${json_list}\"" >> $GITHUB_OUTPUT
  use-list:
    needs: generate_list
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include: ${{fromJson(needs.generate_list.outputs.list)}}

    steps:
      - name: Use the List
        run: |
          generatedList="${{ needs.generate_list.ouputs.list }}"
          echo "Generated list in the next step: $generatedList"
